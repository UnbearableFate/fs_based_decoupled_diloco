#!/bin/bash
#PBS -q debug-g
#PBS -W group_list=xg24i002
#PBS -l select=2:mpiprocs=1
#PBS -l walltime=00:10:00
#PBS -j oe
#PBS -m ae

set -eEuo pipefail
trap 'echo "[ERROR] P03 2-node contract failed at line $LINENO" >&2' ERR

export PAGER=cat MODULES_PAGER=cat LMOD_PAGER=cat
REQUIRED_MODULES=()
for module_name in "${REQUIRED_MODULES[@]}"; do
  module load "$module_name"
done
module list 2>&1 | cat

: "${PBS_NODEFILE:?PBS_NODEFILE is required}"
PROJECT_ROOT="${PROJECT_ROOT:-${PBS_O_WORKDIR:?PBS_O_WORKDIR is not set}}"
PYTHON_BIN="${PYTHON_BIN:-$PROJECT_ROOT/.venv/bin/python}"
RUN_ID="${RUN_ID:-$(date +%Y%m%d_%H%M%S)_duraloco_p03_2node}"
PARENT_RUN_ID="${PARENT_RUN_ID:-}"
ARTIFACT_ROOT="${ARTIFACT_ROOT:-$PROJECT_ROOT/artifacts/duraloco/P03/$RUN_ID}"
STORAGE_ROOT="${STORAGE_ROOT:-$PROJECT_ROOT/runs/duraloco/P03/$RUN_ID}"
ROUNDS="${ROUNDS:-100}"
source "$PROJECT_ROOT/scripts/lib/project_paths.sh"
require_project_paths "$PROJECT_ROOT" "$ARTIFACT_ROOT" "$STORAGE_ROOT"

if [[ ! -x "$PYTHON_BIN" ]]; then
  echo "Python executable not found: $PYTHON_BIN" >&2
  exit 2
fi

cd "$PROJECT_ROOT"
mkdir -p "$ARTIFACT_ROOT/ranks" "$STORAGE_ROOT"
export PYTHONPATH="$PROJECT_ROOT${PYTHONPATH:+:$PYTHONPATH}"

hostname | tee "$ARTIFACT_ROOT/submission_host.log"
sort -u "$PBS_NODEFILE" | tee "$ARTIFACT_ROOT/hosts.log"
printf 'PBS_JOBID=%s\nPBS_NODEFILE=%s\nSTORAGE_ROOT=%s\nROUNDS=%s\n' \
  "${PBS_JOBID:-}" "$PBS_NODEFILE" "$STORAGE_ROOT" "$ROUNDS" \
  | tee "$ARTIFACT_ROOT/pbs_environment.log"
"$PYTHON_BIN" --version 2>&1 | tee "$ARTIFACT_ROOT/python_version.log"
git status --short --branch | tee "$ARTIFACT_ROOT/git_status.log"
git rev-parse HEAD | tee "$ARTIFACT_ROOT/git_commit.log"
findmnt -T "$STORAGE_ROOT" 2>&1 | tee "$ARTIFACT_ROOT/mount.log" || true
if command -v lfs >/dev/null 2>&1; then
  lfs getstripe "$STORAGE_ROOT" 2>&1 | tee "$ARTIFACT_ROOT/stripe.log" || true
else
  printf 'lfs unavailable\n' | tee "$ARTIFACT_ROOT/stripe.log"
fi

MPI_COMMAND=(
  mpirun
  --mca mpi_abort_print_stack 1
  --report-bindings
  --bind-to none
  --map-by ppr:1:node
  -np 2
  /usr/bin/env
  "PROJECT_ROOT=$PROJECT_ROOT"
  "PYTHON_BIN=$PYTHON_BIN"
  "STORAGE_ROOT=$STORAGE_ROOT"
  "ARTIFACT_ROOT=$ARTIFACT_ROOT"
  "RUN_ID=$RUN_ID"
  "ROUNDS=$ROUNDS"
  bash -lc
  'cd "$PROJECT_ROOT"; export PYTHONPATH="$PROJECT_ROOT${PYTHONPATH:+:$PYTHONPATH}"; exec "$PYTHON_BIN" -m fs_diloco.storage.miyabi_contract_worker --root "$STORAGE_ROOT" --prefix "runs/$RUN_ID" --rounds "$ROUNDS" --output-dir "$ARTIFACT_ROOT/ranks"'
)
printf -v MPI_TEXT '%q ' "${MPI_COMMAND[@]}"
printf '%s\n' "${MPI_TEXT% }" | tee "$ARTIFACT_ROOT/commands.log"
"${MPI_COMMAND[@]}" 2>&1 | tee "$ARTIFACT_ROOT/two_node_contract.log"

"$PYTHON_BIN" - "$ARTIFACT_ROOT/ranks" "$ARTIFACT_ROOT/two_node_summary.json" <<'PY'
import json
from pathlib import Path
import sys

rank_dir, output = map(Path, sys.argv[1:])
reports = [json.loads((rank_dir / f"rank_{rank}.json").read_text()) for rank in range(2)]
assert {report["rank"] for report in reports} == {0, 1}
assert len({report["hostname"] for report in reports}) == 2
assert all(report["rounds"] >= 100 for report in reports)
assert all(report["double_winner_rounds"] == 0 for report in reports)
assert all(report["visibility_failures"] == 0 for report in reports)
assert all(report["stale_lock_takeover"] == "pass" for report in reports)
summary = {
    "rounds": reports[0]["rounds"],
    "hosts": sorted(report["hostname"] for report in reports),
    "winner_counts": {str(report["rank"]): report["winner_count"] for report in reports},
    "double_winner_rounds": 0,
    "visibility_failures": 0,
    "stale_lock_takeover": "pass",
}
output.write_text(json.dumps(summary, indent=2, sort_keys=True) + "\n")
print(json.dumps(summary, sort_keys=True))
PY

qstat "$PBS_JOBID" | tee "$ARTIFACT_ROOT/qstat_final.log"
cp "$ARTIFACT_ROOT/two_node_contract.log" "$ARTIFACT_ROOT/stdout.log"
: > "$ARTIFACT_ROOT/stderr.log"

MANIFEST_ARGS=(
  --root "$PROJECT_ROOT"
  --output "$ARTIFACT_ROOT/manifest.json"
  --phase P03
  --purpose contract
  --run-id "$RUN_ID"
  --backend lustre
  --config scripts/miyabi/run_duraloco_p03_storage_2node.pbs
  --commands-log commands.log
  --stdout stdout.log
  --stderr stderr.log
  --exit-code 0
  --result pass
  --assertion "100 two-node same-version CAS races had exactly one winner each"
  --assertion "both nodes observed every winning head and stale-lock takeover passed"
)
if [[ -n "$PARENT_RUN_ID" ]]; then
  MANIFEST_ARGS+=(--parent-run-id "$PARENT_RUN_ID")
fi
"$PYTHON_BIN" scripts/agent/create_run_manifest.py "${MANIFEST_ARGS[@]}"
"$PYTHON_BIN" scripts/agent/check_run_manifest.py "$ARTIFACT_ROOT/manifest.json" \
  --root "$PROJECT_ROOT"
