# PXX Phase Report

## English

### Identity and final-target binding

- Phase:
- Feature branch:
- Actual base branch/commit:
- Final verified implementation commit:
- Checked persistence/archival commit:
- Maker:
- Independent Checker:
- Checker job/run ID:
- Verdict: `PASS | PASS_WITH_FOLLOWUPS | BLOCKED`
- Required-gate follow-ups:

### Acceptance summary

| Acceptance ID | Result | Evidence bound to final commit |
|---|---|---|
| | | |

### Verification and attempt lineage

| Shape/layer | Run/job ID | Result | Parent run ID | Commit/config digest | Artifact |
|---|---|---|---|---|---|
| | | | | | |

Include failed, inconclusive, queued-cancelled, pre-allocation-cancelled, and
successful attempts. A retry must use a new run ID and link the previous
attempt of the same validation shape through `parent_run_id`.

### Changed files and behavior

### Compatibility and migration

### Faults and counterexamples exercised

List at least one exact historical regression and one Checker-added
counterexample rerun on the final verified commit.

### Checker failure history (append-only)

For every blocked/failed attempt record identity/time, phenomenon, expected vs
actual, confirmed reason or `unknown`, affected acceptance IDs/invariants,
evidence, fix, retry lineage, and resolution. Never delete an earlier failure.

### Backend capabilities, supported claims, and non-claims

### Open follow-ups and next action

### Merge state

- Worktree clean at final runtime:
- Branch pushed:
- Current persisted suite/state/report/checksums all green:
- Phase completed:
- Next phase authorized:
- Ready to merge:
- External-risk approval still required:

## 中文

### 身份与最终目标绑定

- 阶段：
- Feature branch：
- 实际 base branch/commit：
- 最终已验证实现 commit：
- 已复核持久化/archival commit：
- Maker：
- 独立 Checker：
- Checker job/run ID：
- 结论：`PASS | PASS_WITH_FOLLOWUPS | BLOCKED`
- 必需 gate follow-ups：

### 验收摘要

| Acceptance ID | 结果 | 绑定最终 commit 的证据 |
|---|---|---|
| | | |

### 验证与尝试 lineage

| Shape/层级 | Run/job ID | 结果 | Parent run ID | Commit/config digest | Artifact |
|---|---|---|---|---|---|
| | | | | | |

必须包含失败、inconclusive、queued-cancelled、allocation 前取消和
成功尝试。重试必须使用新 run ID，并用 `parent_run_id` 指向同一
validation shape 的前一尝试。

### 改动文件与行为

### 兼容性与迁移

### 已执行的故障与反例

在最终 verified commit 上至少重放一个历史精确回归和一个 Checker
新增反例。

### Checker 失败历史（只追加）

对每个 blocked/failed 尝试记录身份/时间、现象、expected vs actual、
已证实原因或 `unknown`、受影响 acceptance/invariant、证据、修复、
重试 lineage 和 resolution。不得删除早先失败记录。

### Backend capabilities、已支持主张与 non-claims

### Open follow-ups 与下一动作

### Merge 状态

- 最终 runtime 时 worktree 是否干净：
- Branch 是否已 push：
- 当前 persisted suite/state/report/checksums 是否全绿：
- Phase 是否完成：
- 是否授权下一 phase：
- Ready to merge：
- 仍需的外部风险批准：
